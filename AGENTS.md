# AGENTS.md — ClipySi for macOS

macOS のクリップボードマネージャ。[Clipy](https://github.com/Clipy/Clipy) の操作感に着想を得た、Apple Silicon ネイティブの **SwiftUI + AppKit** アプリ（Clipy Project とは無関係）。

- 設計の詳細: [DESIGN.md](DESIGN.md)
- セキュリティ要件（capture/persistence/paste/update を触る前に必読）: [security-guidance.md](security-guidance.md)
- ビルド手順・アーキテクチャ概要・依存関係: [README.md](README.md)

## リポジトリ構成
- `Clipy/` … アプリ本体（`Clipy.xcodeproj`）。テストは `ClipyTests/`。
- `Scripts/` … `gate.sh`（マージ前ローカルゲート）/ `redaction-grep.sh`（秘匿性の静的チェック）/ `release-notarize.sh`（署名・公証）/ `release-dmg.sh`（配布用 DMG の作成・公証）。
- `docs/` … 利用者・開発者向けの公開ドキュメント。
- `tools/clipy-realm-export/` … 旧 Clipy（Realm）履歴のエクスポータ（移行用・アプリ本体には非リンク）。
- 暗号・機密マスキング・record/vault フォーマット・同期の共有ロジックは、別リポジトリの Rust コア **`clipy-si-core`**。リリースアプリにはビルド済みバイナリ（XCFramework）として静的リンクされる（実装はこの公開リポジトリには含まれない）。

## ビルド・テスト・Lint
`-skipMacroValidation` は CLI からの `xcodebuild` で常に必要。テストは 14 言語カタログのため `-testLanguage en -testRegion US` を固定する。

```bash
# Debug ビルド
set -o pipefail && xcodebuild -project Clipy.xcodeproj -scheme Clipy -configuration Debug \
  -destination 'platform=macOS,arch=arm64' -skipMacroValidation CODE_SIGNING_ALLOWED=NO build 2>&1 | xcbeautify

# テスト
set -o pipefail && xcodebuild -project Clipy.xcodeproj -scheme Clipy \
  -destination 'platform=macOS,arch=arm64' -skipMacroValidation -testLanguage en -testRegion US \
  CODE_SIGN_IDENTITY=- CODE_SIGNING_REQUIRED=NO CODE_SIGNING_ALLOWED=NO test 2>&1 | xcbeautify

# Lint（ゲートは --strict）
swiftlint lint --quiet

# マージ前ゲート（lint → redaction-grep → test を一括）
./Scripts/gate.sh
```

エラー診断時は `| xcbeautify` を外して生ログを見る。

## 守るべき制約
- **セキュリティは非交渉**（詳細は [security-guidance.md](security-guidance.md)）。クリップボードは全コピー内容（パスワード/トークン含む）を扱う。pasteboard プライバシーマーカー（`ConcealedType` / `TransientType` / `AutoGeneratedType`）付きのクリップは**保存しない**。履歴は保存時に AES-GCM 暗号化（鍵は Keychain・`ThisDeviceOnly`）。秘密情報をログ・テスト・コミットに残さない（`print` / `NSLog` 禁止 — `Scripts/redaction-grep.sh` が静的に強制）。実コピーで capture を試さない。App Sandbox は付けない／Hardened Runtime は Release で必須。
- **内部シンボル名は意図的に `Clipy` のまま**（リネーム禁止）: `PRODUCT_MODULE_NAME=Clipy`・スキーム/ターゲット・`Clipy.xcodeproj`・`Clipy/`・`ClipyTests`・`@testable import Clipy`・`kCPY*` キー。製品名と bundle id のみ ClipySi。
- **永続化される識別子・データ形式・UserDefaults キー・スニペット XML 要素名はオリジナル互換**を保つ（履歴移行のため）。
- ユーザー向け文言は必ず 14 言語カタログ `Clipy/Localizable.xcstrings` へ。
- Lint クリーン（`.swiftlint.yml`）。AppKit/UI に触れるコードは `@MainActor`、共有可変状態は `actor`、ドメインをまたぐ値型は `Sendable`。

## 確定事項
| 項目 | 決定 |
| --- | --- |
| UI | SwiftUI 中心 + AppKit ブリッジ（NSStatusItem / NSPanel） |
| 永続化 | SQLiteData / GRDB（Realm は廃止） |
| 最低 OS | macOS 14 Sonoma |
| アーキテクチャ | arm64 + x86_64 ユニバーサル |
| 配布 | Developer ID + notarization（App Sandbox 不可・MAS 不可） |

## PR の前に
- `./Scripts/gate.sh` を通す。
- セキュリティに関わる変更（capture/persistence/paste/update）は [security-guidance.md](security-guidance.md) の該当項目を確認。
- 挙動互換に関わる変更では、必要に応じてオリジナル Clipy の該当サブシステムを確認する。
- 脆弱性は公開 issue ではなく [SECURITY.md](SECURITY.md) の手順で非公開に報告する。
