# security-guidance.md — Clipy セキュリティガイダンス

> クリップボードマネージャは **ユーザーがコピーした全内容（パスワード・トークン・OTP・秘密鍵・個人情報）を捕捉・保存しうる**、本質的にセンシティブなアプリ。本書は必須要件と、オリジナル実装で**確認済みの問題**＋対策をまとめる。設計全体は [DESIGN.md](DESIGN.md)、運用は [AGENTS.md](AGENTS.md)。
>
> オリジナル実装のファイル参照は、任意のローカル参照 checkout `repos/Clipy/...` 相対。

## 1. 脅威モデル
- **保存される機密**: 履歴は無差別に蓄積される。攻撃面は (a) 同一ユーザーで動く他プロセスによる平文ストア読取、(b) Time Machine / 非暗号化バックアップ、(c) 改竄された永続ファイルの非セキュア deserialization、(d) 自動更新の改竄。
- **高権限**: Accessibility 許可 + `CGEvent` キーストローク注入はシステム全体の入力制御に相当。ここに到達する別の欠陥（例: 安全でない unarchive での任意クラス生成）があると被害が増幅する。
- **多くは「リモート/越境」攻撃ではなく同一ユーザー文脈**だが、機密の常時蓄積という性質上、深刻度は実利用に即して評価する。

## 2. 非交渉の必須要件（リライトで必ず満たす）
- [ ] **R1 pasteboard プライバシーマーカーを尊重**: `org.nspasteboard.ConcealedType` / `TransientType` / `AutoGeneratedType`（＋レガシー同義）付きは保存しない。content を読む前に判定。
- [ ] **R2 NSSecureCoding のみ**: 非セキュア `NSKeyedUnarchiver.unarchiveObject(withFile:/with:)` / `archiveRootObject(_:toFile:)` を排除。
- [ ] **R3 保存時暗号化**: 履歴本体・サムネイル・**DB メタ（`title` プレビュー等の機微列を含む）** を Keychain 保管鍵で暗号化。現実装は機微列のフィールド暗号化＋blob 暗号化を採用する。平文の機微情報を一切残さない。鍵は `...ThisDeviceOnly`。
- [ ] **R4 App Sandbox を付けない / Hardened Runtime を有効化**: entitlements は最小限。notarization 必須。
- [ ] **R5 高権限面のハードニング**: paste 前に貼り付け先 frontmost を再検証。Accessibility のみ要求（Input Monitoring 不要）。
- [ ] **R6 自動更新は EdDSA + HTTPS**: レガシー DSA を削除。
- [ ] **R7 開発衛生**: 秘密をログ/テスト/コミットに残さない。実コピーで capture を試さない。バックアップ除外（`isExcludedFromBackup`）を検討。

## 3. 確認済みの問題と対策（オリジナル → 新実装）

| # | 深刻度 | 問題 | 主な箇所 | 対策(要件) |
| --- | --- | --- | --- | --- |
| F1 | High | プライバシーマーカー未対応で秘密も捕捉・平文保存 | `ClipService.swift:87-105,154-158` | R1+R3 |
| F2 | Medium | concealed 対策が「1Password owner type かつユーザーが除外追加時のみ」。汎用 `ConcealedType` 未対応 | `ExcludeAppService.swift:89-112` | R1 |
| F3 | High | 非セキュア deserialization（`unarchiveObject(withFile:)`、`CPYClipData` が NSSecureCoding 非準拠） | `PasteService.swift:60,101`, `CPYClipData.swift:16,150-169`, `ClipService.swift:155` | R2 |
| F4 | Low | UserDefaults 由来データ（除外アプリ・ホットキー）の非セキュア unarchive | `NSCoding+Archive.swift:16`, `AppEnvironment.swift:77`, `HotKeyService.swift:106,170,174` | R2 |
| F5 | Medium | 履歴を平文でディスク保存（暗号化・file protection 無し） | `ClipService.swift:132,155`, `SQLiteDataDatabase.swift:47-61`, `Realm+Migration.swift:18` | R3 |
| F6 | Medium | Sandbox/Hardened Runtime/entitlements が一切未設定（このままでは notarize 不可） | `Configurations/CodeSigning.xcconfig`, `project.pbxproj` | R4（ClipySi では Developer ID + Hardened Runtime + entitlements なしで公証済み） |
| F7 | Low | `CGEvent` による Cmd+V 注入の前に貼り付け先検証が無い | `PasteService.swift:142-165` | R5 |
| F8 | Low | Accessibility(AX-trusted) による広範な入力制御権限 | `AccessibilityService.swift:21-29` | R4+R5 |
| F9 | Medium | 0.75ms の常時グローバルポーリングで全アプリのコピーを無差別捕捉 | `ClipService.swift:30-41` | R1 + ポーリング間隔見直し |

各論の対策は §4 以降。

## 4. pasteboard プライバシーマーカー（R1・最重要）
[nspasteboard.org](http://nspasteboard.org/) の規約。capture 時、**content を読む前**に `pasteboard.types` を検査:
- `org.nspasteboard.TransientType` → 一切保存しない（瞬間的内容）。
- `org.nspasteboard.ConcealedType` → 既定で保存しない（機密）。任意のオプトインで「暗号化して保存＋表示はマスク」も可。
- `org.nspasteboard.AutoGeneratedType` → 履歴に記録しない（必要なら現在値表示のみ）。
- `org.nspasteboard.source` → 由来 bundle id（情報）。新実装でも自分が書き戻す際に設定する good citizen 行動を。
- レガシー同義: transient = `de.petermaurer.TransientPasteboardType`(TextExpander/Butler), `com.typeit4me.clipping`(TypeIt4Me), `"Pasteboard generator type"`(Typinator) / concealed = `com.agilebits.onepassword`(1Password)。
- 現実は誤タグも多い（Enpass は Concealed/Transient を過剰付与、Grammarly は AutoGenerated）。**「該当したら skip」を安全側の既定**に。「Concealed を暗号化保存」は opt-in。
- **判定はユーザーの除外リストに依存させない**（無条件）。これがオリジナル F1/F2/F9 の核心的欠陥。

```swift
// Clipy/Capture/PrivacyMarkers.swift
extension NSPasteboard.PasteboardType {
  static let nsTransient     = Self("org.nspasteboard.TransientType")
  static let nsConcealed     = Self("org.nspasteboard.ConcealedType")
  static let nsAutoGenerated = Self("org.nspasteboard.AutoGeneratedType")
  static let nsSource        = Self("org.nspasteboard.source")
}
private let legacyTransient: Set<String> = [
  "de.petermaurer.TransientPasteboardType", "com.typeit4me.clipping", "Pasteboard generator type"
]
private let legacyConcealed: Set<String> = ["com.agilebits.onepassword"]

enum CaptureDecision { case record, skip, concealed }
func captureDecision(for pb: NSPasteboard) -> CaptureDecision {
  let raw = Set((pb.types ?? []).map(\.rawValue))
  if raw.contains(.init(NSPasteboard.PasteboardType.nsTransient.rawValue))
      || raw.contains(.init(NSPasteboard.PasteboardType.nsAutoGenerated.rawValue))
      || !raw.isDisjoint(with: legacyTransient) { return .skip }
  if raw.contains(.init(NSPasteboard.PasteboardType.nsConcealed.rawValue))
      || !raw.isDisjoint(with: legacyConcealed) { return .concealed }
  return .record
}
```
> マーカーは content と同じ `clearContents()/declareTypes()` トランザクションで書かれるため、`changeCount` 検出後すぐ `types` を見ればよい。新 OS では `detectPatterns/detectMetadata`（content を読まずに種別判定でき、読取アラートも出さない）を `#available` で併用。

## 5. 保存時暗号化（R3 / F5）
**初期検証の結論（2026-06、確定）**: **SQLCipher 全 DB 暗号化は採用しない**。証拠 — (1) SQLiteData が引き込む標準 GRDB(SPM 製品)に SQLCipher API（`Database.usePassphrase`）が無くコンパイル不可。(2) GRDB の SQLCipher は SPM の **package trait `GRDBCIPHER`** で有効化する方式だが、**trait は依存を最初に宣言した場所でしか有効化できず、推移依存では下流から有効化不可**。SQLiteData は GRDB を trait 無しで宣言（独自 trait は `SQLiteDataTagged` のみ）。よって SQLCipher 化には **SQLiteData の fork が必要**で、保守コストに見合わない。→ **フィールド暗号化（方式 A）を採用**。

採用方針（方式 A・フィールド暗号化）:
- **鍵**: `SecRandomCopyBytes` で 256bit 生成し、Keychain（generic password, `kSecAttrAccessible = kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`）に保管。ThisDeviceOnly で iCloud 同期されずデバイス拘束。バックグラウンドのポーリングからもアクセス可。
- **本体ペイロード・サムネイル**: CryptoKit `AES.GCM.seal` で暗号化し `.write(to:options:[.atomic,.completeFileProtection])`。
- **`title`（機微列）**: 平文 TEXT で持たず **暗号化 BLOB（`titleCipher`）** として保存。メニュー描画時に表示分だけ復号（件数は少数）。検索は復号後にメモリ内で（履歴は `maxHistorySize` で有界）。※spike で実証: plain title は `clipy.sqlite` に平文露出するが、AES-GCM 暗号文には平文が現れず往復可能。
- **dedupe キー**: 単純 SHA-256 ではなく **HMAC-SHA256(Keychain 鍵, 正規化ペイロード)** とし、DB の `contentHash` から「特定内容がコピーされたか」を推測できないようにする（鍵を持つ自アプリだけが照合可能）。
- **Concealed**: 既定 skip。保存する設定時も必ず上記暗号化経路。
- **非機微メタ**（`createdAt`/サイズ/UTType/`sourceBundle`/`isPinned` 等）は平文で可。DB 構造・件数は見えるが機微内容は残らない（R3 の主旨を満たす）。
- ディレクトリ 0700 / ファイル 0600、必要に応じ `URLResourceValues.isExcludedFromBackup`。
- 実装は `Security/HistoryKey` + `EncryptedBlobStore` + `titleCipher`/HMAC スキーマ確定。SQLCipher は将来 SQLiteData を vendor/fork する判断をした場合のみ再検討。

```swift
import CryptoKit
func archiveEncrypted(_ data: Data, to url: URL, key: SymmetricKey) throws {
  let sealed = try AES.GCM.seal(data, using: key).combined!
  try sealed.write(to: url, options: [.atomic, .completeFileProtection])
}
func unarchiveEncrypted(at url: URL, key: SymmetricKey) throws -> Data {
  try AES.GCM.open(AES.GCM.SealedBox(combined: Data(contentsOf: url)), using: key)
}
```

## 6. セキュアコーディング（R2 / F3・F4）
非セキュア API（`unarchiveObject(withFile:/with:)`, `archiveRootObject(_:toFile:)`, `archivedData(withRootObject:)`）を全廃。永続化されるクラスは `NSSecureCoding` 準拠（`static var supportsSecureCoding { true }`）にし、`decodeObject(of:forKey:)` でクラス allowlist を明示。

```swift
// 書込み
let data = try NSKeyedArchiver.archivedData(withRootObject: clip, requiringSecureCoding: true)
// 読込み（ネストするクラスも allowlist に列挙）
let clip = try NSKeyedUnarchiver.unarchivedObject(
  ofClasses: [CPYClipData.self, NSArray.self, NSString.self, NSData.self, NSImage.self],
  from: plain)
```
> オリジナルには良い手本がある: `DraggedData.swift:15-17` は NSSecureCoding 準拠、`CPYSnippetsEditorWindowController.swift:308,323` は `unarchivedObject(ofClasses:from:)` を使用。`CPYClipData` / `CPYAppInfo` / `KeyCombo` 系を同様に直す。**新実装ではそもそも `Codable`/SQLiteData に寄せ、NSCoding 自体を減らす**のが最善。

## 7. 高権限面のハードニング（R5 / F7・F8）
- 貼り付けは CGEvent Cmd+V を維持しつつ: paste 直前に「Clipy メニューを開く前に frontmost だったアプリ」を記録し、post 時点で frontmost が一致するか検証、不一致なら beep して中止（誤爆防止）。suppression interval を最小化。
- **Accessibility のみ要求**。Input Monitoring は要求しない（ホットキーは Carbon `RegisterEventHotKey`=Magnet で TCC 不要、貼り付けは post であって event tap ではない）。
- システムの Accessibility ダイアログは文言変更不可なので、アプリ内オンボーディングで意図を説明。
- Accessibility 権限とその広範さをユーザー向けドキュメントに明記。entitlements を最小化して、万一の別欠陥が AX 制御面へ波及しないようにする。

## 8. 自動更新の信頼（R6）
- **EdDSA(Ed25519) のみ**: `./bin/generate_keys`（秘密鍵は Keychain、git 禁止）→ `SUPublicEDKey`。`SUFeedURL` は **HTTPS**。appcast は `./bin/generate_appcast` で生成（署名手編集禁止）。
- **レガシー DSA を削除**: `SUPublicDSAKeyFile` と `dsa_pub.pem` を撤去。
  - 補足（検証結果）: オリジナルの DSA key は **実害のある脆弱性ではない**。Sparkle 2.9.2 は DSA 検証コードを持たず、`SUPublicEDKey` 設定時は EdDSA 署名必須・DSA は無視されるため「DSA へのダウングレード」攻撃は成立しない。よって**衛生上の削除**（dead config）として扱う。
- 既存ユーザーが Sparkle 1.21 未満を含む可能性や署名証明書変更がある場合のみ、DSA+EdDSA の dual-signed を 1 リリース挟んでから DSA を落とす。
- agent アプリなので Gentle Reminders（Dock バッジ + 通知）で更新提示。

## 9. Sandbox / Hardened Runtime / entitlements / TCC（R4 / F6）
- **App Sandbox は付けない**: paste が Accessibility + `CGEvent.post` 依存で sandbox 非対応。MAS は CGEvent クリップボードマネージャを 2.4.5 で却下した例 → Developer ID で MAS 外配布。
- **Hardened Runtime 必須**（`ENABLE_HARDENED_RUNTIME=YES` / `codesign --options runtime`）。entitlements は最小限にし、現行の ClipySi release は **entitlements なし**で公証済み。SPM 版 Sparkle はビルド時に同一チームで再署名されるため、`com.apple.security.cs.disable-library-validation` は不要だった。CGEvent post 自体に専用 entitlement 不要。
- inner-to-outer 署名、`--deep` 回避、`--timestamp`。
- 将来 CloudKit 同期を出す場合は App Sandbox + iCloud/CloudKit entitlements が必須になり方針が変わる点に留意（現状の paste 機構とトレードオフ）。

## 10. 開発時の運用ルール
- 秘密情報を **ログ出力しない**（クリップ content をデバッグ print しない）。クラッシュレポートに content を含めない。
- capture の動作確認は**使い捨てのダミー文字列**で。実パスワード等を使わない。パスワードマネージャの concealed/transient content が**保存されない**ことを必ず確認（コア要件）。
- テストは in-memory DB + スタブ pasteboard。**実 `NSPasteboard.general` / ユーザー履歴 DB を触らない**。
- 秘密鍵（Sparkle EdDSA、署名証明書、`.p12`/`.cer`/`.mobileprovision`）を **git に入れない**。
- capture/persistence/paste/update を変更したら本書の該当項目を確認し、マージ前にセキュリティ観点のレビューを行う。

## 11. リリース前チェックリスト
- [ ] concealed/transient/auto-generated content が履歴に入らない（password manager 実機確認）。
- [ ] ディスク上の履歴・サムネイル・DB が暗号化されている（平文で grep して秘密が出ない）。
- [ ] 非セキュア unarchive/ archive API がコードに無い（grep: `unarchiveObject(withFile`, `archiveRootObject`, `archivedData(withRootObject:` 単独形）。
- [ ] paste の貼り付け先検証が入っている。
- [ ] `ENABLE_HARDENED_RUNTIME=YES`、entitlements 最小、App Sandbox 無し。
- [ ] Sparkle は EdDSA + HTTPS、DSA/`dsa_pub.pem` 撤去。
- [ ] notarization 成功・staple 済み（`spctl -a -vvv -t exec`, `stapler validate`, `codesign --verify --deep --strict`）。quarantine 付きでクリーン機確認。
- [ ] 秘密鍵・証明書がリポジトリに含まれない。

## 12. 参考（一次情報）
- nspasteboard 規約: http://nspasteboard.org/
- NSSecureCoding / requiringSecureCoding: Apple `NSKeyedArchiver` ドキュメント
- Keychain データ保護 / SQLCipher: Apple Platform Security, zetetic.net SQLCipher
- 新 pasteboard プライバシー（accessBehavior / detectPatterns, macOS 15.4 preview〜): Apple AppKit ドキュメント, mjtsai.com
- Sparkle EdDSA / sandbox / gentle reminders: sparkle-project.org
- 配布/notarization/SMAppService/Hardened Runtime: Apple Developer Forums(Quinn) ほか
